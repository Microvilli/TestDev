module.exports = {
    plugins: [
        {
            resolve: "gatsby-source-dev",
            options: {
                username: 'geocine'
            }
        },
        {
            resolve: "gatsby-source-dev",
            options: {
                username: 'emmawedekind'
            }
        },
        {
            resolve: "gatsby-source-dev",
            options: {
                username: 'nickytonline'
            }
        }
    ]
}
